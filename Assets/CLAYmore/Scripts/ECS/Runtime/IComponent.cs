namespace CLAYmore.ECS
{
    /// <summary>
    /// Marker interface for all ECS data components.
    /// Implementations must contain ONLY data fields/properties — no logic.
    /// </summary>
    public interface IComponent { }
}
